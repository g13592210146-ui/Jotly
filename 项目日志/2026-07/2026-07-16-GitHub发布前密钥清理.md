# GitHub 发布前密钥清理

## 修改文件

- `.gitignore`
- `README.md`
- `Jotly/JotlySecrets.swift`
- `Jotly/Config/LocalSecrets.example.plist`
- DeepSeek、Memory OS、豆包、阿里云和 MIMO 的模型及语音服务文件
- `项目日志/2026-06/2026-06-15-MIMO-ASR流式识别替换.md`

## 变更点

- 删除源码和历史日志中的硬编码 API Key、Access Token。
- 新增统一密钥读取器，按 `UserDefaults -> 环境变量 -> 本地 LocalSecrets.plist` 读取。
- 本机真实配置迁移到被 Git 忽略的 `Jotly/Config/LocalSecrets.plist`。
- 仓库只保留无真实值的配置模板和配置说明。
- 发布使用全新 Git 历史，避免旧提交中的密钥被带到 GitHub。

## 验证

- 当前工作树敏感值扫描无命中。
- `LocalSecrets.plist` 已被 Git 忽略并成功进入本地 App Bundle。
- Generic iOS 构建：`BUILD SUCCEEDED`。
