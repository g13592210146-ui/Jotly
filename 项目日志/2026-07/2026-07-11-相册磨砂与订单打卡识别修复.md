# 相册磨砂与订单打卡识别修复

## 修改文件

- `Jotly/JotlyHomeScreen.swift`
- `Jotly/JotlyDeepSeekClient.swift`

## 变更

- 相册面板移除不透明系统白底，改为单层 `ultraThinMaterial` 磨砂背景。
- 图片请求增加订单与消费凭证的打卡判定：匹配已有习惯时追加打卡，否则创建习惯打卡卡片。
- OCR 和模型直传共用同一图片场景规则，不在客户端写死特定 App 或商户名。

## 验证

- `xcodebuild` generic iOS 构建通过：`BUILD SUCCEEDED`。
