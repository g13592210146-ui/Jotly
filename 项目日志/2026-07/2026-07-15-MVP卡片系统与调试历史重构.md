# MVP 卡片系统与调试历史重构

## 修改文件
- `JotlyModels.swift`、`JotlyHomeScreen.swift`、`JotlyHomeScreenSections.swift`、`JotlyHomeViewModel.swift`
- `JotlyDeepSeekClient.swift`、`JotlyShortcutAnalysisService.swift`
- `JotlyDebugTurnStore.swift`、`JotlyLocalStore.swift`
- `MemoryOS/JotlyDatabase.swift`、`MemoryOS/JotlyDatabaseSchema.swift`、`MemoryOS/MemoryOSDatabaseRecords.swift`
- `Assets.xcassets/AppIcon.appiconset/*`

## 变更点
- 新增常规/开发者模式，常规模式隐藏模型、费用、图片模式和流程调试控件。
- 扩展结构化卡片字段并兼容历史 JSON；统一全宽卡片、处理中动效、胶囊确认选项和精简完成态内容。
- 重做生日、饮品打卡、倒计时、资产、订阅和购物小票卡片；小票子项确认后才执行工具。
- 图片输入保存本地卡片背景文件，资产卡可使用用户图片并覆盖可读性遮罩。
- APP 内和快捷指令共用处理中状态更新，模型完成后在原卡片上转为确认或业务卡。
- 新增卡片级多轮调试历史数据库迁移，保存提示词、模型输出、Token、缓存和流程节点。
- 重新裁切应用图标，去掉源图白边，保持系统负责圆角裁切。

## 验证
- `git diff --check` 通过。
- Generic iOS `xcodebuild` 通过：`BUILD SUCCEEDED`。
- 已从 `codex/card-mvp-ui-20260715` 合并到 `feature/memory-os`；合并前还原点为 `8168e58`。
