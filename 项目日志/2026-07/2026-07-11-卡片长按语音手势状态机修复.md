# 卡片长按语音手势状态机修复

## 修改文件

- `Jotly/JotlyHomeScreenSections.swift`

## 变更

- 移除卡片视图层的重复启动震动，只保留 ASR 就绪后的一次启动震动。
- 卡片长按与拖动改为单一连续手势，避免录音覆盖层出现后打断原手势。
- 录音覆盖层只负责显示，不再抢占触摸事件。
- 手指移出卡片时震动并显示“松手取消”，在卡片外松手只取消当前录音，不提交卡片修改。

## 验证

- `xcodebuild` generic iOS 构建通过：`BUILD SUCCEEDED`。
