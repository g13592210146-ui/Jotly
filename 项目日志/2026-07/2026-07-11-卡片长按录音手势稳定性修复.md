# 卡片长按录音手势稳定性修复

## 修改文件
- `Jotly/JotlyHomeScreen.swift`
- `Jotly/JotlyHomeScreenSections.swift`

## 变更点
- 卡片长按录音期间禁用主卡片列表纵向滚动，避免页面和卡片跟随手指移动。
- 录音覆盖层改为高优先级拖动手势，避免被外层列表手势抢占。
- 保留卡片边界判断：手指在卡片内上下移动不取消，真正移出卡片区域后才显示“松手取消”并触发震动。

## 验证
- 待真机验证长按卡片、卡片内上下移动、移出卡片和松手四个场景。
