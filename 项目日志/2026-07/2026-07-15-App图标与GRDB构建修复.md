# App 图标与 GRDB 构建修复

## 修改文件
- `Jotly/Assets.xcassets/AppIcon.appiconset/Contents.json`
- `Jotly/Assets.xcassets/AppIcon.appiconset/AppIcon-*.png`

## 变更
- 新增蓝绿渐变的 Jotly App 图标，以记忆卡片与语音波形作为核心符号。
- 补齐 iOS 1024 图标与 Mac 各尺寸图标资源。
- 定位并移走 Xcode 默认 DerivedData 中损坏的 GRDB checkout，重新生成 SwiftPM 缓存。
- 使用 Xcode 实际默认缓存重新解析 GRDB 7.11.1，恢复工程产品引用。

## 验证
- App Icon 源图为 1024×1024、无 Alpha 通道。
- 默认 DerivedData 下执行 `xcodebuild` iOS Generic Device 构建通过：`BUILD SUCCEEDED`。
