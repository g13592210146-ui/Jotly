# 2026-06-29 卡片长按取消态与缓存回显修复

## 修改的文件
- `JotlyHomeScreenSections.swift`
- `JotlyHomeScreen.swift`
- `项目日志/README.md`

## 修复的问题与变更点
### 1. 卡片长按语音增加“拖出即取消”
- 给卡片补充录音增加了拖出边界后的取消态识别。
- 手指滑出卡片后会触发震动反馈，并在卡片浮层上显示“取消”。
- 松手时会按当前状态走提交或取消，不再只依赖抬手完成。

### 2. 恢复调试列表里的缓存统计展示
- 调试对话列表此前走的是 `LegacyDebugConversationRow`，没有显示 Token / 缓存统计。
- 切回带 `Token 统计与缓存命中` 的行后，缓存命中、缓存创建和未命中数据恢复可见。

## 验证结果
- `xcodebuild -project /Users/gaoxingchen/Documents/Jotly/Jotly/Jotly.xcodeproj -scheme Jotly -destination 'generic/platform=iOS' -derivedDataPath /private/tmp/JotlyDerivedData CODE_SIGNING_ALLOWED=NO build`
- 结果：`BUILD SUCCEEDED`
