# 2026-06-29 习惯与正倒数日补充模式修复

## 修改的文件
- `JotlyDeepSeekClient.swift`
- `JotlyHomeViewModel.swift`
- `JotlyHomeScreenSections.swift`
- `JotlyToolDispatcher.swift`

## 修复的问题与变更点

### 1. 习惯打卡 / 正倒数日补充误判修复
- 补充模式新增 `habit` 与 `countdown` 的专门上下文说明，明确这两类卡片的后续输入属于同一卡片的继续编辑，不再默认按“无法识别该指令”处理。
- 保持原有卡片补充流程不变，只调整模型对习惯打卡与正倒数日的理解边界。

### 2. 代码层收敛
- 保持现有删除、确认、工具调用逻辑不变，仅做了提示词与局部代码整理。
- 继续保留当前的右滑删除交互，二次确认后续再单独讨论。

## 验证结果
- 项目编译构建成功：
  ```bash
  xcodebuild -project Jotly.xcodeproj -scheme Jotly -destination generic/platform=iOS CODE_SIGNING_ALLOWED=NO build
  ```
  输出：`** BUILD SUCCEEDED **`
