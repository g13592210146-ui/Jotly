# 2026-06-12 接手规范与日志通道

## 记录目的

把当前项目的交接方式和运行日志入口固定下来，避免后续只靠聊天记录继续推进。

## 本次约定

### 1. 文档先行

项目必须维护成“可接手”的状态。新模型或新开发者接手时，优先读取：

- `README.md`
- `AI_USER_STORY.md`
- `DESIGN_GUIDELINES.md`
- `项目日志/` 里的最新记录

### 2. 日志入口

iOS 运行态要通过统一日志出口输出到 Xcode Console，便于从编译和运行两端判断状态。

本次新增统一日志分类：

- `app`
- `speech`
- `deepseek`
- `storage`
- `tool`

### 3. 维护原则

- 需求变化要记录
- 卡点和尝试过的方案要记录
- 不要把关键信息只放在聊天里

## 后续使用方式

后续如果出现新的需求调整、语音链路问题、DeepSeek 解析问题、卡片状态问题，优先：

1. 看 `README.md`
2. 看最近的项目日志
3. 看 Xcode Console
4. 再改代码

## 本次验证

- 已把统一日志入口收敛到 `JotlyLog`
- 已补 repo 级接手说明 `README.md`
- 已用 `xcodebuild` 在 `/private/tmp/JotlyDerivedData` 下完成一次编译验证，结果为 `BUILD SUCCEEDED`
