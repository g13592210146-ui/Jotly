# 2026-06-29 快捷入口合入主分支

## 修改文件
- `Jotly/JotlyShortcuts.swift`
- `Jotly/JotlyHomeViewModel.swift`
- `Jotly/JotlyHomeScreen.swift`
- `项目日志/README.md`

## 变更点
- 合入隔离工作树中的 App Intents 快捷入口能力。
- 新增两个系统快捷动作：`截图` 和 `语音`。
- `截图` 动作接收快捷指令传入的图片数据，并复用现有图片分析链路。
- `语音` 动作打开应用后复用现有语音输入链路。
- 在应用内快捷入口页提供两个“去添加”按钮，跳转系统快捷指令创建页。

## 验证结果
- 已在主工作树执行真机 iPhone 构建：
  `xcodebuild -project /Users/gaoxingchen/Documents/Jotly/Jotly/Jotly.xcodeproj -scheme Jotly -destination 'platform=iOS,id=00008140-001C782934F1801C' build`
- 结果：`BUILD SUCCEEDED`。
- 构建产生的 `project.pbxproj` 注释/排序噪音已还原，避免混入无关工程文件变更。
