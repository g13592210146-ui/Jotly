# 2026-06-27 千问系列显式上下文缓存接入

## 本次修改
- [JotlyDeepSeekClient.swift](/Users/gaoxingchen/Documents/Jotly/Jotly/Jotly/JotlyDeepSeekClient.swift)

## 解决的问题
- 仅对 `qwen3.7-plus`、`qwen3.6-flash`、`qwen3.5-flash` 的请求消息改成千问上下文缓存支持的显式写法。
- 将稳定的主系统提示词拆成 `content` 数组中的缓存块，并在首块上加 `cache_control: { type: "ephemeral" }`。
- 生日专项提示词继续作为后置普通块发送，不加缓存标记，避免把可复用前缀和场景分支混在一起。

## 验证结果
- `xcodebuild -project /Users/gaoxingchen/Documents/Jotly/Jotly/Jotly.xcodeproj -scheme Jotly -destination 'generic/platform=iOS' -derivedDataPath /private/tmp/JotlyDerivedData CODE_SIGNING_ALLOWED=NO build`
- 结果：`BUILD SUCCEEDED`
