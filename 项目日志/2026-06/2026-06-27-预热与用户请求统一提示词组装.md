# 2026-06-27 预热与用户请求统一提示词组装

## 修改的文件
- [JotlyDeepSeekClient.swift](/Users/gaoxingchen/Documents/Jotly/Jotly/Jotly/JotlyDeepSeekClient.swift)
- [项目日志/README.md](/Users/gaoxingchen/Documents/Jotly/Jotly/项目日志/README.md)

## 修复的问题与变更点
- 把千问系列的 prompt 组装收敛成同一个 `makePromptBundle` 入口，预热和真实请求不再各自拼 prompt。
- 预热请求改为走同样的用户上下文格式，避免和后续正常对话在结构上分叉。
- 生日技能提示继续作为独立技能层挂载，`我生日 / 我的生日` 仍然默认指用户本人，不再被示例带偏。

## 验证结果
- 真机直接构建时，当前环境没有识别到 `00008140-001C782934F1801C` 这台 iPhone。
- 通用 iOS 编译验证通过：
  ```bash
  xcodebuild -project Jotly.xcodeproj -scheme Jotly -destination 'generic/platform=iOS' -derivedDataPath /private/tmp/JotlyDerivedData CODE_SIGNING_ALLOWED=NO build
  ```
  结果：`** BUILD SUCCEEDED **`
