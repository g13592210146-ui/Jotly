# 2026-06-29 合并核验与图片直传修复

## 修改的文件
- `JotlyDeepSeekClient.swift`
- `JotlyHomeViewModel.swift`
- `JotlyHomeScreen.swift`
- `JotlyHomeScreenSections.swift`
- `JotlyModels.swift`
- `项目日志/README.md`

## 修复的问题与变更点
### 1. 恢复图片直传调试链路
- 将 `DeepSeekClient` 的调试请求重新接回 `imageAttachment` 与 `imageInputMode`，让图片输入路径和现有语音文本路径保持一致。
- 补齐调试面板里对图片输入模式的可见性，避免合并后只剩文本提示而丢失图片上下文。

### 2. 补齐缓存创建字段
- 在 `AgentModelUsage` 和 `AgentDebugTurn` 中补入缓存创建 token 的传递与解码。
- 调试卡片只在接口返回时展示缓存创建，避免空值也占位。

### 3. 修正合并后的卡片参数顺序
- 调整 `CardListRow` / `TaskCardView` 的构造参数顺序，恢复主界面编译。

## 验证结果
- `xcodebuild -project /Users/gaoxingchen/Documents/Jotly/Jotly/Jotly.xcodeproj -scheme Jotly -destination 'generic/platform=iOS' -derivedDataPath /private/tmp/JotlyDerivedData CODE_SIGNING_ALLOWED=NO build`
- 结果：`BUILD SUCCEEDED`
