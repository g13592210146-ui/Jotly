# 2026-06-30 快捷指令卡片首帧与结果回显修复

## 修改文件
- `Jotly/JotlyShortcuts.swift`

## 修复问题
- 修复快捷指令刚弹出系统卡片时先出现空白、只有系统“完成”按钮的问题。
- 修复系统卡片轮询 `latestShortcutOperation` 导致结果可能不回显或被其他快捷操作干扰的问题。

## 变更点
- 快捷指令启动分析时直接返回本次 `ShortcutAnalysisOperation`。
- `ShortcutAnalysisSnippetView` 初始化时直接绑定本次 operation，首帧立即显示“正在处理”内容。
- snippet 后续轮询固定的 `operationId`，只刷新本次快捷分析的状态和结果。

## 验证结果
- 已执行：
  `xcodebuild -project /Users/gaoxingchen/Documents/Jotly/Jotly/Jotly.xcodeproj -scheme Jotly -destination 'generic/platform=iOS' -derivedDataPath /private/tmp/JotlyDerivedData CODE_SIGNING_ALLOWED=NO build`
- 结果：`BUILD SUCCEEDED`
