# 2026-06-30 快捷指令系统卡片按钮收敛

## 修改文件
- `Jotly/JotlyShortcuts.swift`
- `Jotly/JotlyShortcutAnalysisService.swift`

## 修复问题
- 去掉 snippet 内部自定义的“取消 / 打开 APP 查看结果”按钮，避免与系统固定“完成”按钮形成两套按钮且点击不稳定。
- 快捷指令系统卡片现在只负责展示处理中、失败原因和完成结果。
- 处理中说明改为：点击系统“完成”只是收起面板，不会取消后台处理，结果会回写到 APP 首页卡片。
- 语音入口未收到听写文本时，错误文案明确提示需要先添加系统“听写文本”，再把结果传给 Jotly“语音”。

## 验证结果
- 已执行：
  `xcodebuild -project /Users/gaoxingchen/Documents/Jotly/Jotly/Jotly.xcodeproj -scheme Jotly -destination 'generic/platform=iOS' -derivedDataPath /private/tmp/JotlyDerivedData CODE_SIGNING_ALLOWED=NO build`
- 结果：`BUILD SUCCEEDED`
