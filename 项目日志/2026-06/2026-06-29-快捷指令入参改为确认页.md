# 2026-06-29 快捷指令入参改为确认页

## 修改文件
- `Jotly/JotlyShortcuts.swift`
- `Jotly/JotlyHomeViewModel.swift`
- `Jotly/JotlyHomeScreen.swift`

## 变更点
- 将 `语音` 快捷动作改为接收 Shortcuts 的听写文本参数。
- 将 `截图` 快捷动作改为接收截图图片参数。
- 新增 App 内确认页：截图先预览图片，语音先预览文本。
- 点击 `完成` 后才进入现有图片分析或文本分析链路；`取消` 直接丢弃本次输入。

## 验证结果
- 已执行 iOS 构建验证，结果 `BUILD SUCCEEDED`。
