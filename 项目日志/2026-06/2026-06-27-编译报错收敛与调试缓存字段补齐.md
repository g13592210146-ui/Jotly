# 2026-06-27 编译报错收敛与调试缓存字段补齐

## 修改的文件
- `Jotly/JotlyHomeScreen.swift`
- `Jotly/CloudASRServices.swift`
- `Jotly/JotlyModels.swift`
- `Jotly/JotlyHomeViewModel.swift`
- `Jotly/JotlyHomeScreenSections.swift`
- `Jotly/项目日志/README.md`

## 修复的问题与变更点
### 1. 收敛旧版重复定义，恢复工程可编译
- 将 `JotlyHomeScreen.swift` 和 `CloudASRServices.swift` 中保留的旧实现统一改成 `Legacy*`，避免与拆分后的新文件重复声明。
- 将云端 ASR 的 `Data` / `UInt32` / `Int32` 辅助扩展改名，避免和 `ASRBufferExtensions.swift` 冲突。

### 2. 补齐调试卡片的 token 数据来源
- 在 `AgentDebugTurn` 中加入每轮 token 统计字段。
- 在模型返回后把 `promptTokens`、`completionTokens`、`totalTokens`、`promptCacheHitTokens`、`promptCacheMissTokens` 写回调试 turn。
- 调试卡片只展示接口实际返回的字段，移除不存在的“缓存创建”展示项。

## 验证结果
- `xcodebuild -project "/Users/gaoxingchen/Documents/Jotly/Jotly/Jotly.xcodeproj" -scheme Jotly -destination 'generic/platform=iOS' -derivedDataPath /private/tmp/JotlyDerivedData CODE_SIGNING_ALLOWED=NO build`
- 结果：`BUILD SUCCEEDED`
- 仍有若干 Swift 6 并发警告，但不影响本次编译通过。
