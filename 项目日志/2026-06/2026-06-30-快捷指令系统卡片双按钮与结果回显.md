# 2026-06-30 快捷指令系统卡片双按钮与结果回显

## 修改文件
- `Jotly/JotlyShortcuts.swift`
- `Jotly/JotlyShortcutAnalysisService.swift`
- `Jotly/JotlyLocalStore.swift`
- `Jotly/JotlyHomeViewModel.swift`
- `Jotly/项目日志/README.md`

## 变更内容
- 把快捷指令系统卡从临时 `session` 改成 `operationId` 驱动的持久化状态。
- 处理中状态增加 `取消` 按钮，完成状态增加 `打开 APP 查看结果` 按钮，两个按钮都由 `AppIntent` 驱动。
- 截图 / 语音继续共用同一套分析服务、调试流和落库流。
- `LocalStore` 新增快捷操作读写能力，`JotlyHomeViewModel` 在收到结果打开请求后切回卡片模式。
- 修复 `JotlyStoreSnapshot` 新增字段后的旧数据兼容解码问题，避免老的 `jotly_store.json` 因缺少 `shortcutOperations` 读取失败。

## 验证结果
- 已执行 `xcodebuild -project /Users/gaoxingchen/Documents/Jotly/Jotly/Jotly.xcodeproj -scheme Jotly -destination 'generic/platform=iOS' -derivedDataPath /private/tmp/JotlyDerivedData CODE_SIGNING_ALLOWED=NO build`
- 结果：`BUILD SUCCEEDED`
